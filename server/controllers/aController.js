const models = require('../models/aModel.js');
const fs = require('fs');
const path = require('path');
const aController = {};


aController.findSomething = (req, res, next) => {

};

//add controllers for DB works
module.exports = aController